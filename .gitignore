__pycache__/
.pytest_cache/
.venv/
**/.venv/
dist/
outputs/*
*.egg-info
output_stage*
*.egg-info
**/outputs/
tea_debug.log

# runlog command history (local, not tracked)
.runlog.jsonl

# TruckDrive training run output (wandb, checkpoints; local, not tracked)
recipes/alpamayo1_5_sft/output_truckdrive/

# Run outputs / W&B state / baseline artifacts (local, not tracked)
output_*/
**/wandb/

# Window-index caches (regenerated from the dataset; ~1 MB of JSON)
**/truckdrive_windows_cache_*.json

# Local environment (contains a W&B API key -- never commit)
recipes/alpamayo1_x_rl/env.sh

# Personal scratch notes / one-off debug renders
handoff.md
*.png

# Raw training-run stdout logs (local, not tracked)
recipes/alpamayo1_5_sft/logs/
